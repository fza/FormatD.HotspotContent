{
  "name": "formatd-hotspot-content",
  "description": "Self-contained frontend bundle for FormatD.HotspotContent",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "NODE_ENV=production node esbuild.js",
    "watch": "node esbuild.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "esbuild-sass-plugin": "^3",
    "sass": "^1.77.0"
  }
}
